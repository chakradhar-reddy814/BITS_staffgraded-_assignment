# Q5 Explanation

- `lsblk` listed the available storage devices and partitions present in the environment.
- `mount | head -5` showed the current mounted filesystems and their mount points.
- `df -h` reported disk space consumption and availability, which helps assess storage health.
- `df -i` reported inode utilization, which is useful for identifying whether the system is running out of filesystem entries.
